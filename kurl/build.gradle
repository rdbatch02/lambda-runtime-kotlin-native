apply plugin: 'konan'

konan.targets += ['linux_x64', 'macos_x64']

konanArtifacts {
    interop('libcurl') {
    }

    library('kurl') {

        libraries {
            artifact 'libcurl'
        }
    }
}